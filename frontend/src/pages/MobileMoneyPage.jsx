import { useState } from 'react'
import API from '../api/client'
export default function MobileMoneyPage(){const [form,setForm]=useState({provider:'ecocash',amount:'1.00',currency:'USD',customer_phone:''});const [result,setResult]=useState(null);const [error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');try{const r=await API.post('/mobilemoney/initiate/',form);setResult(r.data)}catch{setError('Could not initiate mobile money request.')}};return <div><h2>Mobile Money Gateway</h2>{error?<div className="alert alert-error">{error}</div>:null}<div className="content-card"><form onSubmit={submit} className="form-grid"><select value={form.provider} onChange={e=>setForm({...form,provider:e.target.value})}><option value="ecocash">EcoCash</option><option value="innbucks">InnBucks</option><option value="mukuru">Mukuru</option><option value="zipit">ZIPIT</option></select><input value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/><input value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}/><input placeholder="Customer phone" value={form.customer_phone} onChange={e=>setForm({...form,customer_phone:e.target.value})}/><button className="btn btn-primary">Create Prompt</button></form></div>{result?<pre>{JSON.stringify(result,null,2)}</pre>:null}</div>}
