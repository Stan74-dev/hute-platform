import { useEffect, useState } from 'react'
import API from '../api/client'
export default function FiscalisationPage(){const [invoices,setInvoices]=useState([]);const [error,setError]=useState('');useEffect(()=>{API.get('/fiscalisation/invoices/').then(r=>setInvoices(r.data.results||r.data||[])).catch(()=>setError('Could not load fiscal invoices.'))},[]);return <div><h2>ZIMRA Fiscalisation</h2><p>Fiscal invoice tracking and middleware integration status.</p>{error?<div className="alert alert-error">{error}</div>:null}<div className="content-card"><h3>Fiscal Invoices</h3><table><thead><tr><th>Invoice</th><th>Status</th><th>Currency</th><th>Total</th><th>Fiscal Receipt</th></tr></thead><tbody>{invoices.map(row=><tr key={row.id}><td>{row.invoice_number}</td><td>{row.status}</td><td>{row.currency}</td><td>{row.gross_total}</td><td>{row.fiscal_receipt_number||'-'}</td></tr>)}{invoices.length===0?<tr><td colSpan="5">No fiscal invoices yet.</td></tr>:null}</tbody></table></div></div>}
